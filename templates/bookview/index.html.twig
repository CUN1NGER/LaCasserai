{% extends 'base.html.twig' %}

{% block title %}Hello BookViewController!{% endblock %}

{% block body %}
{{ "Bookings starting today\n"|nl2br }}
{% for booking in booking %}
    {{ booking.checkinDate ? booking.checkinDate|date('d-m-Y') : '' }}
{% endfor %}

{{"\n------------------- \n Bookings ending today \n"|nl2br}}
    {% for yeet in yeet %}
        {% if yeet == NULL %}
        yeet
        {% else %}
        {{ yeet.checkoutDate ? yeet.checkoutDate|date('d-m-Y') : '' }}
        {% endif %}
    {% endfor %}

{% endblock %}